# Generated by the package manager. Reformatting it churns on every install and
# the format is pnpm's to decide, not ours.
pnpm-lock.yaml

# Build output.
.next/
out/

# The copy for /ki-wirkung as it was handed over. It is a record of the source
# text, so it stays byte-for-byte as received.
docs/ki-wirkung-copy.md

# Article sources. Prettier's markdown printer rewrites the JSX embedded in MDX
# and does not always produce something MDX can still parse: running it over
# these left `claude-code-essentials-foundations` with a </div> closing across a
# list item and `first-month-as-solution-architect` with an expression acorn
# could not read, and the build failed on both. The prose is hand-written and
# wants to stay as typed anyway.
*.mdx
