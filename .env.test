# Test Environment Variables
# These are used during testing to mock external services

# Next.js
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Supabase - Test/Mock credentials (not real)
NEXT_PUBLIC_SUPABASE_URL=https://test.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=test-anon-key

# Resend - Test/Mock API key (not real)
RESEND_API_KEY=re_test_key

# Plausible Analytics - Disabled in tests
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=localhost

# Newsletter List ID (mock)
RESEND_AUDIENCE_ID=test-audience-id
